_ph = _ph || {};

_ph.current = _ph.current || {};

_ph.current.project = _ph.current.project || {};

(function ($, dbg, app, undefined) {

    // most of the project related stuff.
    // - check changes in project state on state updates.
    // - update the state and keep account of current project
    // - 
     
 })(jQuery, debug, _ph);